# FakeMicro 前端管理界面

基于 Vue 3 + TypeScript + Element Plus 构建的微服务管理平台前端界面。

## 功能特性

- 🎯 **现代化界面**: 基于 Vue 3 和 Element Plus 的现代化 UI 设计
- 📱 **响应式布局**: 完美适配桌面端和移动端
- 🔐 **权限管理**: 基于角色的细粒度权限控制
- 📊 **数据可视化**: 集成 ECharts 提供丰富的数据图表
- 🌙 **主题切换**: 支持亮色/暗色主题切换
- 🌍 **国际化**: 支持多语言（开发中）
- ⚡ **高性能**: 基于 Vite 的快速构建和热更新

## 技术栈

- **框架**: Vue 3 + TypeScript
- **UI组件**: Element Plus
- **路由**: Vue Router 4
- **状态管理**: Pinia
- **构建工具**: Vite
- **图表库**: ECharts
- **HTTP客户端**: Axios
- **样式预处理器**: SCSS

## 项目结构

```
frontend/
├── public/                 # 静态资源
├── src/
│   ├── assets/            # 静态资源（图片、字体等）
│   ├── components/        # 公共组件
│   ├── layouts/          # 布局组件
│   ├── router/           # 路由配置
│   ├── services/         # API 服务
│   ├── stores/           # 状态管理
│   ├── types/            # TypeScript 类型定义
│   ├── utils/            # 工具函数
│   ├── views/            # 页面组件
│   ├── App.vue           # 根组件
│   └── main.ts           # 入口文件
├── package.json
├── vite.config.ts        # Vite 配置
└── tsconfig.json         # TypeScript 配置
```

## 快速开始

### 环境要求

- Node.js >= 16.0.0
- npm >= 7.0.0 或 yarn >= 1.22.0

### 安装依赖

```bash
cd frontend
npm install
```

### 开发环境

```bash
# 启动开发服务器
npm run dev

# 访问 http://localhost:3000
```

### 生产构建

```bash
# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

## 页面功能

### 1. 登录页面
- 用户登录/注册
- JWT Token 认证
- 自动刷新 Token

### 2. 仪表板
- 系统概览统计
- 用户增长趋势图表
- 消息发送统计图表
- 最近活动时间线

### 3. 用户管理
- 用户列表查看
- 用户信息编辑
- 角色权限管理
- 用户状态管理

### 4. 消息管理
- 消息发送记录
- 消息状态跟踪
- 多渠道消息管理
- 消息详情查看

### 5. 文件管理
- 文件上传下载
- 文件信息管理
- 文件权限控制

### 6. 系统监控
- 系统性能指标
- 服务健康状态
- 实时监控图表

## API 集成

前端通过 RESTful API 与后端服务通信：

### 认证相关
- `POST /api/auth/login` - 用户登录
- `POST /api/auth/register` - 用户注册
- `POST /api/auth/logout` - 用户登出
- `POST /api/auth/refresh` - 刷新 Token
- `GET /api/auth/me` - 获取当前用户信息

### 用户管理
- `GET /api/users` - 获取用户列表
- `POST /api/users` - 创建用户
- `PUT /api/users/{id}` - 更新用户
- `DELETE /api/users/{id}` - 删除用户

### 消息管理
- `GET /api/messages` - 获取消息列表
- `POST /api/messages` - 发送消息
- `GET /api/messages/{id}` - 获取消息详情
- `PUT /api/messages/{id}` - 更新消息状态

## 权限控制

系统支持三种角色权限：

### 用户角色
- **User**: 普通用户，只能查看自己的信息
- **Admin**: 管理员，可以管理用户和消息
- **SystemAdmin**: 系统管理员，拥有所有权限

### 权限配置

在路由配置中设置权限要求：

```typescript
{
  path: '/users',
  name: 'Users',
  component: () => import('@/views/Users.vue'),
  meta: { 
    requiresAuth: true, 
    title: '用户管理', 
    roles: ['Admin', 'SystemAdmin'] 
  }
}
```

## 主题定制

### 颜色主题

项目支持亮色和暗色主题切换：

```scss
// 亮色主题
:root {
  --primary-color: #409eff;
  --bg-color: #ffffff;
  --text-primary: #303133;
}

// 暗色主题
html.dark {
  --primary-color: #409eff;
  --bg-color: #141414;
  --text-primary: #e5eaf3;
}
```

### 自定义主题

可以通过修改 SCSS 变量来自定义主题：

```scss
// src/styles/variables.scss
$--color-primary: #409eff;
$--color-success: #67c23a;
$--color-warning: #e6a23c;
$--color-danger: #f56c6c;
```

## 开发指南

### 添加新页面

1. 在 `src/views/` 目录下创建 Vue 组件
2. 在 `src/router/index.ts` 中添加路由配置
3. 在侧边栏菜单中添加导航项

### 添加新组件

1. 在 `src/components/` 目录下创建组件
2. 使用 TypeScript 定义 Props 类型
3. 添加组件文档和示例

### API 集成

1. 在 `src/services/` 目录下创建 API 服务
2. 在 `src/types/api.ts` 中定义数据类型
3. 在组件中调用 API 服务

## 部署指南

### 开发环境部署

```bash
# 构建开发版本
npm run build:dev

# 或使用 Docker
docker build -t fakemicro-frontend:dev .
```

### 生产环境部署

```bash
# 构建生产版本
npm run build

# 使用 Nginx 部署
docker run -d -p 80:80 -v $(pwd)/dist:/usr/share/nginx/html nginx:alpine
```

### 环境变量配置

复制 `.env.example` 为 `.env` 并修改配置：

```bash
cp .env.example .env
```

## 常见问题

### 1. 依赖安装失败

```bash
# 清除缓存重新安装
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### 2. 类型检查错误

```bash
# 运行类型检查
npm run type-check

# 或使用 Vue TSC
npx vue-tsc --noEmit
```

### 3. 构建失败

```bash
# 检查 TypeScript 配置
npm run build -- --mode development

# 查看详细错误信息
npm run build -- --debug
```

## 贡献指南

1. Fork 本项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开 Pull Request

## 许可证

本项目采用 MIT 许可证。

## 联系方式

- 项目主页: [GitHub Repository]
- 问题反馈: [Issues]
- 邮箱: [your-email@example.com]

---

**注意**: 本项目仍在积极开发中，API 和功能可能会有变动。